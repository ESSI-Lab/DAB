/**
 * This module provides a set of objects related to the <a href="../classes/GINode.html#accessOptions" class="crosslink">options</a> 
 * of the {{#crossLink "GINode/accessLink:method"}}GINode <code>accessLink</code> method{{/crossLink}} 
 * 
 * @module AccessOptions
 **/

/**
 * 
 * <pre><code> 
 * // resolution in EPSG:4326, each value is expressed in degrees
 * var firstAxisSize = {
     "label": "Lat",
     "value": 600
  };<br>
  var secondAxisSize = {
     "label": "Lon",
     "value": 600
  };<br>
  // resolution in EPSG:3857, each value is expressed in metres
  var firstAxisSize = {
     "label": "X",
     "value": 1000
  };<br>
  var secondAxisSize = {
     "label": "Y",
     "value": 1200
  }; 
   </pre></code>
 * 
 * @class AxisSize
 **/

/**
 *  
 * @property label
 * @type {String}
 **/

/**
 *
 * @property value
 * @type {Double}
 **/