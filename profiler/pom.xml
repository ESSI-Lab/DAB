<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>eu.essi_lab</groupId>
		<artifactId>DAB</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>

	<name>ESSI-Lab Profiler</name>
	<artifactId>profiler</artifactId>
	<packaging>pom</packaging>

	<modules>
		<module>profiler-oaipmh</module>
		<module>profiler-opensearch</module>
		<module>profiler-openmetrics</module>
		<module>profiler-esri</module>
		<module>profiler-bnhs</module>
		<module>profiler-ckan</module>
		<module>profiler-csw</module>
		<module>profiler-semantic</module>
		<module>profiler-cdi</module>
		<module>profiler-pub-sub</module>
		<module>profiler-wof</module>
		<module>profiler-wps</module>
		<module>profiler-wfs</module>
		<module>profiler-rest</module>
		<module>profiler-rest-views</module>
		<module>profiler-arpa-rest</module>
		<module>profiler-observations</module>
		<module>profiler-arpa-hydrocsv</module>
		<module>profiler-sos</module>
		<module>profiler-terms</module>
		<module>profiler-wis</module>
		<module>profiler-wms</module>
		<module>profiler-wms-extent</module>
		<module>profiler-wms-cluster</module>
		<module>profiler-thredds</module>
		<module>profiler-gwis</module>
		<module>profiler-ssc-feed</module>
		<module>profiler-worldcereal</module>
	</modules>

	<dependencies>

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>access-tools</artifactId>
			<version>${project.parent.version}</version>
		</dependency>

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>database-indexes</artifactId>
			<version>${project.parent.version}</version>
		</dependency>

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>core-datamodel</artifactId>
			<version>${project.parent.version}</version>
		</dependency>

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>core-messages</artifactId>
			<version>${project.parent.version}</version>
		</dependency>

		<dependency>
			<groupId>${project.parent.groupId}</groupId>
			<artifactId>profiler-dev-kit</artifactId>
			<version>${project.parent.version}</version>
		</dependency>

		<dependency>
			<groupId>org.apache.cxf</groupId>
			<artifactId>cxf-rt-frontend-jaxrs</artifactId>
		</dependency>

		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>
		
		<dependency>
			<groupId>javax.xml.bind</groupId>
			<artifactId>jaxb-api</artifactId>
		</dependency>

		<dependency>
			<groupId>org.json</groupId>
			<artifactId>json</artifactId>
		</dependency>

	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>${project.parent.groupId}</groupId>
				<artifactId>gs-lib-odip</artifactId>
				<version>${project.parent.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.parent.groupId}</groupId>
				<artifactId>gs-lib-what3words</artifactId>
				<version>${project.parent.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>


</project>